/** @jsx jsx */
import { jsx } from 'theme-ui'

const SearchText = () => (
    <span
        sx={{
            fontFamily: 'heading',
            display: 'inline-block',
            fontSize: 1,
            mr: 2,
            color: 'text'
        }}
    >
        View stories by
    </span>
)

export default SearchText